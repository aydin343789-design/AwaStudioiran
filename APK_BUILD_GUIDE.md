# راهنمای جامع دریافت فایل نصبی اندروید (APK) و استقرار
# Comprehensive Android APK Build & Deployment Guide

اپلیکیشن **آوا استودیو TTS Pro** یک برنامه موبایلی پیشرفته با معماری PWA و Capacitor است که به شما امکان تبدیل متن فارسی و انگلیسی به صوت، اعراب‌گذاری هوشمند، تنظیم سرعت و لحن، خروجی WAV و اجرای آفلاین را می‌دهد.

---

## 🚀 روش اول: ساخت خودکار فایل APK در GitHub Actions (بدون نیاز به نصب نرم‌افزار)

این پروژه از قبل دارای فایل اکشن گیت‌هاب در مسیر `.github/workflows/build-apk.yml` است. با مراحل زیر فایل APK به صورت خودکار برای شما کامپایل می‌شود:

### مراحل:
1. در حساب [GitHub.com](https://github.com) خود وارد شوید و یک مخزن (Repository) جدید ایجاد کنید (مثلاً `awastudio-tts`).
2. کدهای این پروژه را در ترمینال سیستم خود کامیت و پوش کنید:
```bash
git init
git add .
git commit -m "Initial release of AwaStudio TTS Pro"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/awastudio-tts.git
git push -u origin main
```
3. پس از اجرای دستور `git push`، به صفحه ریپازیتوری خود در وب‌سایت گیت‌هاب بروید و روی تب **Actions** کلیک کنید.
4. کار (Workflow) با عنوان **Build Android APK** به صورت خودکار آغاز می‌شود.
5. پس از حدود ۳ الی ۵ دقیقه و ظاهر شدن تیک سبز، روی اجرا کلیک کنید و از بخش **Artifacts** در پایین صفحه، فایل زیپ حاوی **`app-debug.apk`** را مستقیماً دانلود و روی گوشی‌های اندروید نصب کنید!

---

## 🛠️ روش دوم: بیلد محلی در سیستم با Capacitor و Android Studio

اگر اندروید استودیو روی رایانه شما نصب است:

```bash
# ۱. ساخت خروجی فرانت‌اند
npm run build

# ۲. نصب نیازمندی‌های Capacitor
npm install @capacitor/core @capacitor/cli @capacitor/android

# ۳. افزودن و همگام‌سازی اندروید
npx cap add android
npx cap sync android

# ۴. باز کردن در اندروید استودیو
npx cap open android
```

سپس در منوی بالای Android Studio گزینه:
`Build > Build Bundle(s) / APK(s) > Build APK(s)`
را انتخاب کنید. فایل خروجی در پوشه زیر ذخیره می‌شود:
`android/app/build/outputs/apk/debug/app-debug.apk`

---

## 🌐 روش سوم: تبدیل آنلاین و سریع با PWABuilder (۲ دقیقه)

1. پروژه را روی هر هاست ابری (مثل Vercel، Liara، Netlify یا Render) مستقر کنید.
2. آدرس دامنه خود را در [PWABuilder.com](https://www.pwabuilder.com) وارد کنید.
3. استاندارد‌های PWA به طور کامل پاس می‌شوند (تیک‌های سبز).
4. روی دکمه **Package for Android** کلیک کرده و فایل نصبی APK یا بسته Google Play (AAB) را دانلود فرمایید.

---

## 📱 قابلیت‌های کلیدی برنامه (Features)
- 🎙️ **موتور صوتی دو زبانه**: پشتیبانی کامل از فارسی (`fa-IR`) و انگلیسی (`en-US`, `en-GB`).
- 🔤 **اعراب‌گذاری و تصحیح هوشمند**: افزودن خودکار فتحه، کسره، ضمه، تنوین، تشدید و نیم‌فاصله برای پیشگیری از اشتباه‌خوانی کلمات هم‌نگاشت.
- 🔢 **تبدیل هوشمند اعداد به حروف**: جلوگیری از خواندن ارقام به صورت انگلیسی یا نامفهوم.
- ⚡ **تنظیم دقیق سرعت و لحن**: سرعت ۰.۵x تا ۲.۰x و فرکانس صدای بم تا زیر با قالب‌های لحن آماده (رادیویی، کتاب صوتی، گوینده خبر، آموزشی، داستانی).
- 💾 **خروجی صوتی باکیفیت**: دانلود فایل صوتی استاندارد `.WAV` (16-bit PCM)، `.webm` یا `.mp3`.
- 📤 **اشتراک‌گذاری سریع**: ارسال مستقیم فایل صوتی به تلگرام، واتساپ، ایتا، بله، روبیکا یا ذخیره در فایل‌ها.
- 📶 **کارکرد ۱۰۰٪ آفلاین**: بدون نیاز به اینترنت با استفاده از Web Speech API و Service Worker داخلی PWA.
